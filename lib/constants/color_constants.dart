import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const Color primary = Color(0xFFd0bdd8);
  static const Color secondary = Color(0xFFE0D4FF);
  static const Color accent = Color(0xFFD4F1FF);

  // Background gradient
  static const Color gradientStart = Color(0xFFFFE0E9);
  static const Color gradientMiddle = Color(0xFFE0D4FF);
  static const Color gradientEnd = Color(0xFFD4F1FF);

  // Text colors
  static const Color textPrimary = Colors.black87;
  static const Color textSecondary = Colors.black54;

  // Glow / effect
  static const Color glow = Colors.pinkAccent;

  // Common
  static const Color white = Colors.white;
}
